/**
 * Build version marker — bump ทุกครั้งที่มี breaking change ใน native code
 * เพื่อให้ผู้ใช้ตรวจได้ว่า APK ที่ install อยู่เป็น version ที่ตรงกับ commit ไหน
 *
 * แสดงในหน้า Map → tab Upload Images → คู่กับ status native EXIF module
 */
export const APP_BUILD = '2026-04-28-native-exif-v1';
