# แผนที่: ไม่ต้องใช้ API key ใด ๆ
# แอปใช้ OpenStreetMap + Leaflet ผ่าน WebView (ฟรี, ไม่ต้องเปิด billing)

# AES Encryption - fallback master key (production ต้องใช้ Keychain/Keystore)
APP_MASTER_SALT=change-me-in-production

# Backend API (ถ้ามี)
API_BASE_URL=https://api.example.com
